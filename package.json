{
  "name": "jira-ng",
  "version": "0.0.0",
  "description": "",
  "author": "f4bio",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "npx rollup --environment BUILD:development --config --watch",
    "clean": "npx del-cli dist",
    "lint": "npx eslint --ext .ts,.tsx .",
    "ci": "npm run lint",
    "build:prod": "npx rollup --environment BUILD:production --config",
    "build:dev": "npx rollup --environment BUILD:development --config",
    "build": "npm run clean && npm run ci && npm run build:prod"
  },
  "dependencies": {
    "@babel/runtime": "^7.22.10",
    "@violentmonkey/dom": "^2.1.5",
    "@violentmonkey/ui": "^0.7.8",
    "slugify": "^1.6.6"
  },
  "devDependencies": {
    "@babel/plugin-transform-react-jsx": "^7.22.5",
    "@gera2ld/plaid": "^2.6.0",
    "@gera2ld/plaid-rollup": "^2.6.0",
    "@rollup/plugin-node-resolve": "^15.2.1",
    "@rollup/plugin-replace": "^5.0.2",
    "@unocss/postcss": "^0.55.2",
    "@unocss/preset-icons": "^0.55.2",
    "@unocss/preset-uno": "^0.55.2",
    "@violentmonkey/types": "^0.1.5",
    "del-cli": "^5.0.0",
    "eslint": "^8.47.0",
    "eslint-plugin-prettier": "^5.0.0",
    "prettier": "^3.0.2",
    "rollup-plugin-userscript": "^0.2.0",
    "unocss": "^0.55.2"
  }
}
